<div class="row">
    <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
        <div class="card">
            <h5 class="card-header">
                <div class="row">
                    <div class="col">
                        <span>دسته بندی ها</span>
                    </div>
                    <div class="col d-flex">
                        <a href="/admin/category/create" class="btn btn-sm btn-primary mr-auto">دسته بندی جدید</a>
                    </div>
                </div>
            </h5>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-striped table-bordered first">
                        <thead>
                            <tr>
                                <th>عنوان دسته بندی</th>
                                <th>جایگاه دسته بندی</th>
                                <th>تنظیمات</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                        <% categories.docs.forEach(category => { %>
                        <tr>
                            <td><%- category.name %></td>
                            <td>
                                <%- category.parent ? category.parent.name : 'دسته اصلی' %>
                            <td>
                                <div>
                                    <form action="/admin/category/<%- category._id %>?_method=DELETE" method="POST">
                                        <input type="hidden" name="_method" value="delete">
                                        <a href="/admin/category/<%= category._id %>/edit" class="btn btn-sm btn-primary">ویرایش</a>
                                        <button type="submit" class="btn btn-sm btn-danger">حذف</button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                        <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
            <%- include(viewPath('layouts/paginate'), { pagination : categories }) %>
        </div>
    </div>
</div>